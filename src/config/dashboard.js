/**
 * BMS Dashboard — Project Configuration
 *
 * Central place for layout constants, thresholds, and project-wide settings.
 * Import specific values into pages/components as needed.
 */

// ─── Card Height Tokens ────────────────────────────────────────────────────
// Applied to the Card element (total card height incl. header).

/** Extra-large cards — radar, complex multi-element layouts */
export const CARD_H_LG = 440

/** Large chart cards — trend lines, doughnut, grouped bar */
export const CARD_H = 400

/** Medium cards — sentiment splits, driver lists, ranking bars */
export const CARD_H_MD = 320
